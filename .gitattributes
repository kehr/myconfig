# MyConfig .gitattributes

# 自动检测文本文件并设置合适的行尾符
* text=auto

# 明确标记文本文件
*.md text
*.txt text
*.py text
*.toml text
*.sh text eol=lf
*.bash text eol=lf
*.zsh text eol=lf

# 明确标记二进制文件
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.tar.gz binary
*.tgz binary
*.dmg binary
*.pkg binary

# Shell 脚本在所有平台使用 LF 行尾
bin/* text eol=lf
contrib/_myconfig text eol=lf

# Python 文件
*.py text diff=python

# 配置文件
*.toml text
*.json text
*.xml text
*.plist text

# 文档文件
*.md text diff=markdown
*.rst text
*.txt text

# 日志文件（如果被包含的话）
*.log text

# 防止 macOS 系统文件被提交
.DS_Store binary
._.DS_Store binary
**/.DS_Store binary
**/._.DS_Store binary

# Git LFS（如果将来需要处理大文件）
# *.zip filter=lfs diff=lfs merge=lfs -text
# *.tar.gz filter=lfs diff=lfs merge=lfs -text
