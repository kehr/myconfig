# MyConfig .gitattributes

# Auto detect text files and perform LF normalization
* text=auto

# Explicitly mark text files
*.txt text
*.md text
*.py text
*.sh text
*.toml text
*.json text
*.xml text
*.plist text
*.yml text
*.yaml text

# Explicitly mark binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary
*.tgz binary
*.dmg binary
*.app binary

# Shell scripts use LF line endings on all platforms
*.sh text eol=lf
install.sh text eol=lf
uninstall.sh text eol=lf

# Python files
*.py text diff=python

# Configuration files
*.toml text
*.json text

# Documentation files
*.md text
*.txt text
README.md text

# Log files (if included)
*.log text

# Prevent macOS system files from being committed
.DS_Store filter=lfs diff=lfs merge=lfs -text
.Spotlight-V100 filter=lfs diff=lfs merge=lfs -text
.Trashes filter=lfs diff=lfs merge=lfs -text

# Git LFS (if needed for large files in the future)
# *.tar.gz filter=lfs diff=lfs merge=lfs -text